# Step 02 — Settings page copy

Translate all user-facing strings owned by `SettingsPage.tsx`: Appearance controls, Git, shortcuts section shell, runners shell, Local CLI, Account/password flow, archived sessions, placeholders, button text, dialog text, and ARIA labels.

Keep product names, branch examples, user content, paths, versions, palette brand names, and backend-provided error messages unchanged.
